{% extends 'base.html.twig' %}

{% block title %}ArmePersonnage{% endblock %}

{% block body %}
    <h1>ArmePersonnage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ arme_personnage.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_arme_personnage_index') }}">back to list</a>

    <a href="{{ path('app_arme_personnage_edit', {'id': arme_personnage.id}) }}">edit</a>

    {{ include('arme_personnage/_delete_form.html.twig') }}
{% endblock %}
